import { GpsNewEntityPlaceDirective } from './gps-new-entity-place.directive';

describe('GpsNewEntityPlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new GpsNewEntityPlaceDirective();
    expect(directive).toBeTruthy();
  });
});
